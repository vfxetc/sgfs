condition: any(self['step'][x] in steps_to_include for x in ('code', 'short_name'))

steps_to_include:
    - Animation
    - Anm
    - Groom
    - Light
    - Lighting
    - Matchmove
    - Matchmove Rotomation
    - Matchmove Character
    - Rotomation
    - Model
    - Modeling
    - Sculpt
    - FX
    - Texture
    - Texturing
    - Previs
    - Rigging
    - Rig
    

templates:
    maya_scene_publish:  published/{publisher.type}/{publisher.name}/v{publisher.version:04d}
    maya_camera_publish: published/{publisher.type}/{publisher.name}/v{publisher.version:04d}
    maya_reference_edits_publish: published/{publisher.type}/{publisher.name}/v{publisher.version:04d}
    maya_light_rig_publish: published/{publisher.type}/{publisher.name}/v{publisher.version:04d}
    maya_geocache_publish: published/{publisher.type}/{publisher.name}/v{publisher.version:04d}
