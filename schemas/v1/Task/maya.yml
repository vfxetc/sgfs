type: include

condition: any(self['step'][x] in steps_to_include for x in ('code', 'short_name'))

steps_to_include:
    - Animation
    - Anm
    - Light
    - Match Move Rotomation
    - Model
    