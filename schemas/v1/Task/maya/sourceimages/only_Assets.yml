type: include

condition: |
    if Task['entity']['type'] != 'Asset':
        return False
    return any(self['step'][x] in steps_to_include for x in ('code', 'short_name'))

steps_to_include:
    - Light
    - Texture
    - Model
    - Modelling
    
