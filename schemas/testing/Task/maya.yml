condition: any(self['step'][x] in steps_to_include for x in ('code', 'short_name'))

steps_to_include:
    - Anm
    - Light
    - Model
    
templates:
    maya_workspace: ""
    maya_scene_publish: scenes/published/{publish_type}/{name}/v{version:04d}
    