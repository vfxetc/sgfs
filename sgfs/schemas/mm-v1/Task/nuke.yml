condition: self['project']['id'] in projects_to_include and self['step']['short_name'] in steps_to_include.get(self['step']['entity_type'], {})

projects_to_include: [112] # SitG

steps_to_include:

    Shot:
        - comp

