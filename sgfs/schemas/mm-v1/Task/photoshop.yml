condition: self['step']['short_name'] in steps_to_include.get(self['step']['entity_type'], {})

steps_to_include:

    Asset:
        - texture
        - surface
    Shot:
        - art
        - matte_painting
        - mograph

    # BookPage
    CustomEntity21:
        - layout

