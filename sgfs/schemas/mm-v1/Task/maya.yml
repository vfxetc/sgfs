condition: self['step']['short_name'] in steps_to_include.get(self['step']['entity_type'], {})

steps_to_include:
    Sequence:
        - story
    Asset:
        - model
        - rig
    Shot:
        - anim
        - fx
        - layout
        - light
        - render

templates:

    maya_workspace: .
