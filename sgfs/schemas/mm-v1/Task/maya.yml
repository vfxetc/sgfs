condition: self['step']['short_name'] in steps_to_include

steps_to_include:
    # Asset pipeline
    - Model
    # Shot pipeline
    - Anim
    - FX
    - Layout
    - Light

templates:

    maya_workspace: .
